name: Test Action

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

permissions:
  contents: read

jobs:
  test-action:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Test TK-TODO Check Action  # IGNORE:TK
        id: todo-check
        uses: ./
        with:
          fail-on-found: "false"
          exclude-patterns: "README.md action.yml"

      - name: Check test results
        run: |
          echo "Test completed successfully"
          echo "Found count: ${{ steps.todo-check.outputs.found-count }}"
